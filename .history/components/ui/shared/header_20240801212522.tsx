import React from 'react';

interface Props {

}

export const Header: React.FC<Props> = (props: Props) => {
    return (
        <header className='cn("'>
            Header
        </header>
    );
}